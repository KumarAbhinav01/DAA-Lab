//Write a program to do quick sort using non-recursion

#include<iostream>
using namespace std;

void quicksort(int a[10], int n)
{
    int temp, n, m;
    for(int i=0; i<n; i++)
        temp=a[i];
    

}

int main()
{
    int n,a[100];
    cout<<"\nEnter number of elements to be entered: ";
    cin>>n;
    cout<<"Enter the elements one by one: ";
    for(int i=0; i<n; i++)
        cin>>a[i];
    
    
    
    return 0;
}